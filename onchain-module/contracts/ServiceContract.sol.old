// SPDX-License-Identifier: MIT
pragma solidity ^0.8;

library ServiceContract {

    function addAssetQuantity(string memory, string memory) public returns (bool, bytes memory) { revert("never call this library"); }

    function addPeer(string memory, string memory) public returns (bool, bytes memory) { revert("never call this library"); }

    function addSignatory(string memory, string memory) public returns (bool, bytes memory) { revert("never call this library"); }

    function appendRole(string memory, string memory) public returns (bool, bytes memory) { revert("never call this library"); }

    function createAccount(string memory, string memory, string memory) public returns (bool, bytes memory) { revert("never call this library"); }
	
    function createAsset(string memory, string memory, string memory) public returns (bool, bytes memory) { revert("never call this library"); }

    function createDomain(string memory,string memory) public returns (bool, bytes memory) { revert("never call this library"); }

    function detachRole(string memory,string memory) public returns (bool, bytes memory) { revert("never call this library"); }

    function removePeer(string memory) public returns (bool, bytes memory) { revert("never call this library"); }

    function removeSignatory(string memory,string memory) public returns (bool, bytes memory) { revert("never call this library"); }

    function setAccountDetail(string memory,string memory,string memory) public returns (bool, bytes memory) { revert("never call this library"); }

    function setAccountQuorum(string memory,string memory) public returns (bool, bytes memory) { revert("never call this library"); }

    function subtractAssetQuantity(string memory,string memory) public returns (bool, bytes memory) { revert("never call this library"); }

    function transferAsset(string memory,string memory,string memory,string memory,string memory) public returns (bool, bytes memory) { revert("never call this library"); }

    function getAccount(string memory) public returns (bool, bytes memory) { revert("never call this library"); }

    function getBlock(string memory) public returns (bool, bytes memory) { revert("never call this library"); }

    function getSignatories(string memory) public returns (bool, bytes memory) { revert("never call this library"); }

    function getAssetBalance(string memory,string memory) public returns (bool, bytes memory) { revert("never call this library"); }

    function getAccountDetail() public returns (bool, bytes memory) { revert("never call this library"); }

    function getAssetInfo(string memory) public returns (bool, bytes memory) { revert("never call this library"); }

    function getRoles() external returns (string[] memory) { revert("never call this library"); }

    function getRolePermissions(string memory) public returns (bool, bytes memory) { revert("never call this library"); }

    function getPeers() public returns (bool, bytes memory) { revert("never call this library"); }

}
